### General Guidelines

## Stack 
- Frontend: React Native (Expo), TypeScript strict

## Hard Rules
- Never install a new dependency without asking first
- Never modify the database schema without showing the migration plan
- Environment variables go in .env, never hardcoded

## Testing
- Always leave testing after implementation to me; no need to start or run the app after making changes.